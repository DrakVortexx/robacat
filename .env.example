# Neon PostgreSQL — paste from Neon dashboard → Connection details → Production branch
# Example: postgresql://user:password@ep-xxxx.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=

# Local server port (Render sets PORT automatically)
PORT=3847
